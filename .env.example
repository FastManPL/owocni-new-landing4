# Opcjonalnie: inny adres zapisu niż domyślny /api/cennik (ten sam serwer).
# NEXT_PUBLIC_FORM_SUBMIT_URL=/api/cennik

# Marker.io — skrypt NIE ładuje się przy wejściu na stronę. Widget dopiero po:
#   ?marker=1 w URL, Shift+Alt+M, lub w konsoli: await window.__owocniLoadMarker?.()
# Bez tej zmiennej komponent MarkerOnDemand nic nie robi.
# NEXT_PUBLIC_MARKER_PROJECT_ID=
