═══════════════════════════════════════
EVIDENCE PACK — kinetic
═══════════════════════════════════════

AUDYT WEJŚCIA (P1): PASS
  namespace:     OK — wszystkie selektory z prefixem #kinetic-section
  lifecycle:     OK — return { kill, pause, resume, _s }
  triaż:         Typ B potwierdzony (5× gsap.ticker.add niezależne od ST)
  scroll split:  N/A (brak velocity/physics opartego o getRawScroll)
  exceptions:    ENT-SCROLL-API-01 EXCEPTION — P3-WILL-REPLACE tag na scrollTo/scrollOn/scrollOff

CPU GATING (P2A): Ścieżka 1 (Typ B, IO → pause()/resume())
  mechanizm:        IO → pause()/resume()
  gatingTarget:     [data-gating-target] = .content-wrapper
  rootMargin:       clamp(200–1200px, 0.5 × visualViewport.height) px
  viewport source:  visualViewport.height (fallback: innerHeight)
  recreate on:      orientationchange + visualViewport.resize [TAK]
  pin note:         PIN-DISABLE-01 PASS — pause() NIE dotyka ST (tylko gsap.ticker.remove + tl.pause)

AUTO-FIXy ZASTOSOWANE (P2A):
  1. TS-LINT-UNUSED-01: const root = container — usunięto (dead variable)
  2. TS-LINT-UNUSED-01: const initialBlue = "..." — usunięto (dead variable)
  3. CPU Gating Ścieżka 1: dodano _recreateIO(), _ioCallback(), _onVVResize, _onOrientChange
     + visualViewport listener (named, INP-LEAK-01 ✓) + orientationchange listener
  4. ST-REFRESH-01: dodano _stIo (section-in-view, jednorazowy IO)
     + _settleTimer (layout-settle, 1000ms setTimeout)
     Oba wpisane do observers[] + timerIds[] + cleanups[]
  5. DEV overlay: dodano za if(DEBUG_MODE) z KILL/INIT AGAIN/PAUSE + telemetry 400ms
     cleanup: _ovl.remove() w cleanups[] → INIT AGAIN dostaje czysty slate
  6. GSAP CDN: upgrade 3.12.5 → 3.12.7
  7. Shell boot: kolejność 1:1 z oryginałem (Lenis PIERWSZY → config → lenis.on → ticker.add)
  8. Boot trigger: window.load (nie DOMContentLoaded) — fonty załadowane przed init()

BRAMKI BEZPIECZEŃSTWA:
  B-CPU-03  [REVIEW]  idempotencja pause/resume:        OK — if(_paused) return / if(!_paused) return
  B-CPU-04  [REVIEW]  listener options:                  OK — brak addEventListener w resume() z opcjami (gsap.ticker/scrollOn nie mają options)
  B-CPU-05  [REVIEW]  cleanup order:                     OK — pause() → cleanups → gsapInstances(ST.kill+revert+kill) → timerIds → observers → state reset
  B-VEL-01  [—]       velocity reset resume:             N/A — brak velocity/physics loop
  B-VEL-02  [—]       velocity init:                     N/A
  B-REF-01  [STATIC]  requestRefresh:                    OK — brak ST.refresh() w ticker/RAF/onUpdate; sekcja używa scrollRuntime.requestRefresh() jednorazowo
  B-REF-02  [STATIC]  refresh kaskada:                   OK — brak requestRefresh w onRefresh/onRefreshInit
  B-GEO-01  [STATIC]  geometry:                          N/A — sekcja nie zmienia wysokości (100lvh stała)
  B-PIN-01  [REVIEW]  pin refresh:                       OK — ST-REFRESH-01 scaffold dodany (section-in-view + layout-settle)
  B-CSS-01  [STATIC]  CSS vs GSAP konflikty:             OK — brak CSS transition na właściwościach animowanych przez GSAP
  B-ISO-01  [—]       isolation:                         isolation_override: NIE (owner decision — finalne)
                                                         Sekcja działa poprawnie bez isolation.
                                                         Nowy stacking context = potencjalne ryzyko w Makro-Sekcji (KINETIC pod FAKTY).
                                                         Filozofia MUSI DZIAŁAĆ STABILNIE: nie dodawaj bez powodu.
  CLN-01    [RUNTIME]  cleanup:                          PENDING (owner: kliknij KILL w ?debug=1)
  ST-01     [REVIEW]  ST idempotent callbacks:            OK — onEnter/onLeave: _s.activate()/_s.hibernate() (boolean, idempotentne)
  INP-01    [STATIC]  passive listeners:                  OK — wheel/touch przez ST.observe() (internal passive); resize z { passive: true }
  INP-02    [STATIC]  pointermove:                        N/A — brak pointermove per-frame
  MED-01    [STATIC]  media attributes:                   N/A — brak video/img
  VAN-SPLIT [REVIEW]  SplitText:                          N/A (własna splitIntoChars — brak GSAP SplitText.revert(); nie idempotentna)
  J-GPU-01  [REVIEW]  GPU cleanup:                        OK — tweeny scrubowane (nie one-shot); will-change dynamiczny per-block
  SM-01     [RUNTIME]  INIT AGAIN 3×:                    PENDING (owner: kliknij INIT AGAIN w ?debug=1)
  SM-02     [REVIEW]  double callbacks:                   OK — ST-01 pokrywa (onEnter boolean, zero duplikacji analytics)
  INIT-CPU-01 [REVIEW] long task init:                    WARNING — createParticles(): ~1000–2000 iter SoA init (Float32Array). Jednorazowy — ~20–50ms akceptowalny.
  GL-CPU-01 [REVIEW]  GL render loop:                    N/A — isWebGL: false
  GL-CPU-02 [REVIEW]  GL pause:                          N/A
  GL-MEM-01 [REVIEW]  GL dispose:                        N/A
  GL-WARM-01 [REVIEW] WARM state:                        N/A
  GL-LC-01  [REVIEW]  lifecycle COLD/WARM/HOT/OFF:       N/A
  GL-IDLE-01 [REVIEW] rIC fallback:                      N/A
  GL-MEM-02 [REVIEW]  GL resume:                         N/A
  GL-TIER-01 [REVIEW] GL tier0 fallback:                 N/A
  GL-OFF-01 [MANIFEST-CHECK] timed dispose:              N/A — isWebGL: false
  B-LC-RET-01 [STATIC] return handle:                    OK — return { kill, pause, resume, _s } jako ostatnia instrukcja
  ST-CLEAN-01 [REVIEW] ST cleanup scope:                 OK — kill() iteruje gsapInstances: tl.scrollTrigger.kill() + tl.revert() + tl.kill()
  INP-LEAK-01 [STATIC] named handlers:                   OK — armMobileResizeLock, _resizeParticle, resize, _resizeTunnel, resizeBlobCanvas, positionNigdyGlow, _onVVResize, _onOrientChange — wszystkie named
  INIT-DOM-01 [STATIC] DOMContentLoaded w init:          OK — brak DOMContentLoaded/window.load wewnątrz init()
  DEV-DEL-01  [REVIEW] overlay side-effects:             OK — cały overlay za if(DEBUG_MODE); telemetry clearInterval w cleanups[]; _ovl.remove() w cleanups[]
  HTML-FONTSIZE-01 [STATIC] html/root font-size:         OK — brak w CSS sekcji
  NULL-GUARD-01    [STATIC] null guards:                 OK — b3?.querySelector ✓ (v42 patch); canvas if(!canvas) return ✓; nigdyText/nigdyGlow guard ✓
  IO-SAFE-01       [STATIC] IO entries guard:            OK — _ioCallback: entries[0]?.isIntersecting ✓; _stIo: entries[0]?.isIntersecting ✓
  ARRAY-ASSIGN-01  [STATIC] dynamic array index:         N/A

GUARD PRESERVATION [P2A-03]:
  preserveExistingGuards: true
  Guardy wykryte przed hardningiem:
    1. _sectionVisible + _tickSectionGate (ST onEnter/onLeave → activate/hibernate)
    2. freezeFinal + _freezeClampBusy (scroll clamp guard na SNAP3)
    3. mobileResizeLock + mobileResizeTimer (mobile toolbar guard)
    4. adaptiveDPR._scrollLockUntil (DPR lock podczas scrollu)
    5. if (!nigdyText || !nigdyGlow) return w positionNigdyGlow
    6. if (!canvas) return / if (!ctx) return w particle + cylinder IIFEs
    7. _s._killed guard w każdym ticker
  Guardy zachowane: TAK — żaden nie tknięty przez hardening
  Guardy dodane przez P2A: _s._killed w _recreateIO() (IO recreate guard)

PERFORMANCE (P2A):
  PERF-01   [—]       media attributes:    N/A — brak mediów
  PERF-02   [—]       contain layout:      SKIP — sekcja ma overflow:visible + canvas wychodzące poza bounds; contain layout uciąłoby canvas
  PERF-03   [DEV]     telemetria:          INIT [?]ms | ST [1] | DOM [~70 elementów + ~200 anim-char spans]
                                           Otwórz reference.html?debug=1 → overlay → sprawdź wartości
  PERF-W6   [REVIEW]  5× gsap.ticker.add: WARNING (celowe, zarządzane przez tickerFns[] + _sectionTickOk gate)
  Pozostałe PERF-W*:  CLEAN

ANTI-FOUC (P3 wypełni):
  PERF-04   [—]       inline styles JSX:  PENDING (P3)

DYNAMIC IMPORT (P3 wypełni):
  PERF-05   [—]       dynamic import:     NIE — owner decision (finalne)
  Uzasadnienie: KINETIC jest częścią Makro-Sekcji (FAKTY+KINETIC).
  Wrapper wymaga synchronicznie: _s.milestones, _s._externalScrollTrigger, _s.activate()/_s.hibernate().
  Lazy load KINETIC = milestones undefined przy init wrappera = snap geometry martwa.
  dynamicImport może dotyczyć CAŁEJ Makro-Sekcji na poziomie wrappera — nie KINETIC osobno.
  DI-HERO-01: N/A (dynamicImport: false)

STACK-HARNESS: WYGENEROWANY
  Warunek: hasPin: true + geometryContract: 'pinned-narrative' (≠ 'normal')
  scrollRuntime: stub (window.scrollY) — INT-* testują ST drift, NIE scrollRuntime policy
  INT-01  [RUNTIME]   accordion expand/collapse:  PENDING (owner)
  INT-02  [RUNTIME]   fake-pinned above:          PENDING (owner)
  INT-03  [RUNTIME]   fake-above height change:   PENDING (owner)
  INT-04  [RUNTIME]   positioning invariance:     PENDING (owner)

EXTERNAL CDN LIBRARIES:
  https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.7/gsap.min.js              → gsap
  https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.7/ScrollTrigger.min.js    → ScrollTrigger
  https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js → Lenis
  https://fonts.googleapis.com/css2?family=Lexend:wght@100..900               → (CSS, nie JS)

GSAP VERSION NOTE:
  reference.html + stack.html: 3.12.7 (CDN pinned przez P2A)
  Produkcja: 3.12.7 (npm — środowisko spójne)
  Smoke test w repo wymagany po konwersji React (P3, DEVELOPER_HANDOFF.md)

SECTION POSITION INVARIANCE:
  sensitiveTo geometry-above:  TAK
  trigger_root:                PASS (trigger: container)
  offset_model:                viewport-relative ("top top")
  refresh_mechanism:           refreshSignals: ['section-in-view', 'layout-settle'] (P2A AUTO-FIX)
  ST-REFRESH scaffold:         AUTO-FIXED (P2A: section-in-view IO + layout-settle setTimeout)
  INT-04 positioning test:     PENDING (owner)

WERDYKT: PASS — wszystkie STATIC/REVIEW bramki OK, RUNTIME do weryfikacji przez właściciela

═══ RAW P2A EVIDENCE (verbatim) ═══

Slug: kinetic
Typ: B
CPU Gating: Ścieżka 1 (Typ B, pin:true + niezależne pętle), rootMargin: clamp(200–1200px, 0.5×VH)

AUTO-FIXy zastosowane:
1. TS-LINT-UNUSED-01: usunięto const root = container (dead variable)
2. TS-LINT-UNUSED-01: usunięto const initialBlue = "..." (dead variable)
3. DEBUG_MODE: zachowany — P2A dodaje DEV overlay który go konsumuje
4. CPU Gating Ścieżka 1: dodano _recreateIO(), _ioCallback(), _onVVResize, _onOrientChange,
   visualViewport.addEventListener (named) + cleanup, orientationchange + cleanup
5. ST-REFRESH-01: dodano _stIo (section-in-view, jednorazowy) + _settleTimer (layout-settle 1000ms)
   Oba wpisane do observers[]/timerIds[] i cleanups[]
6. DEV overlay: dodano za if(DEBUG_MODE) — KILL, INIT AGAIN, PAUSE/RESUME, telemetria 500ms

Bramki hardeningu:
B-CPU-01   [AUTO-FIX]  IO gating dodany — Ścieżka 1 (IO-safe, pin — no disable)
PIN-DISABLE-01 [STATIC] PASS — pause() używa gsap.ticker.remove + tl.pause; zero ST.disable()/kill()
B-LC-RET-01 [STATIC]   PASS — return { kill, pause, resume, _s } jako ostatnia instrukcja
B-CPU-03   [REVIEW]    PASS — pause(): if(_paused) return; resume(): if(!_paused) return; idempotentne
B-CPU-04   [REVIEW]    PASS — bez addEventListener w resume() z options
B-CPU-05   [REVIEW]    PASS — kill(): pause() → cleanups → gsapInstances(scrollTrigger.kill+revert+kill) → timerIds → observers → state reset
B-VEL-01   [AUTO-FIX]  N/A
B-VEL-02   [AUTO-FIX]  N/A
B-REF-01   [STATIC]    PASS — brak ST.refresh() w ticker/RAF/onUpdate
B-REF-02   [STATIC]    PASS — brak refresh kaskady
B-GEO-01   [STATIC]    N/A
B-PIN-01   [REVIEW]    PASS — ST-REFRESH-01 scaffold dodany
GL-*       N/A — isWebGL: false
B-CSS-01   [STATIC]    PASS — brak CSS transition na właściwościach GSAP
HTML-FONTSIZE-01 [STATIC] PASS
B-ISO-01   [AUTO-FIX]  OWNER-DECISION-PENDING(blend/filter)
CLN-01     [RUNTIME]   PASS — zweryfikowane: pin-spacer znika, brak orphan styles
ST-01      [REVIEW]    PASS
ST-02      [STATIC]    PASS — trigger: container, start: "top top"
ST-CLEAN-01 [REVIEW]   PASS
INP-01     [STATIC]    PASS
INP-02     [STATIC]    N/A
INP-LEAK-01 [STATIC]   PASS
INIT-DOM-01 [STATIC]   PASS
MED-01     [STATIC]    N/A
DEV-DEL-01  [REVIEW]   PASS
VAN-SPLIT  [REVIEW]    N/A
J-GPU-01   [REVIEW]    PASS
SM-01      [RUNTIME]   PASS — INIT AGAIN 3× zweryfikowane: liczniki stabilne
SM-02      [REVIEW]    PASS
INIT-CPU-01[REVIEW]    WARNING — createParticles(): ~1000–2000 iteracji Float32Array. Jednorazowy ~20–50ms.
TS-LINT-UNUSED-01      PASS (po AUTO-FIX)
NULL-GUARD-01          PASS
IO-SAFE-01             PASS
ARRAY-ASSIGN-01        N/A
PERF-W6: 5× ticker.add — WARNING (celowe)

CPU GATING koegzystencja:
Sekcja ma wewnętrzny ST-native gating (onEnter→activate/onLeave→hibernate).
Fabryka dodała zewnętrzny IO gating (Ścieżka 1: pause()/resume()).
Koegzystencja bezpieczna: B-CPU-03 PASS + ENT-LC-06 PASS.

GUARD PRESERVATION [P2A-03]:
preserveExistingGuards: true
Guardy zachowane: TAK. Guardy dodane: _s._killed w _recreateIO().

RUNTIME (po testach właściciela):
CLN-01: PASS — pin-spacer znika po KILL, brak orphan styles
SM-01:  PASS — INIT AGAIN 3× liczniki stabilne
PAUSE:  PASS — animacje stoją, scroll działa
RESUME: PASS — powrót z właściwego miejsca scruba
FREEZE: PASS — SNAP3 blokuje scroll (celowe, Block 4 przejmuje)

═══ KONIEC RAW P2A EVIDENCE ═══
═══════════════════════════════════════
