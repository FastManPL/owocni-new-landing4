═══════════════════════════════════════
EVIDENCE PACK — kinetic
═══════════════════════════════════════

AUDYT WEJŚCIA (P1): PASS
  namespace:     OK (wszystkie selektory #kinetic-section scoped)
  lifecycle:     OK (init(container) ✓, cleanups[]/gsapInstances[] ✓, return { kill, pause, resume } ✓)
  triaż:         Typ B potwierdzony (4× gsap.ticker.add z per-frame canvas rendering)
  scroll split:  N/A

CPU GATING (P2A): Ścieżka 1
  mechanizm:        IO→pause/resume
  gatingTarget:     data-gating-target (.content-wrapper)
  rootMargin:       0.5×VH clamp(200, 1200)px
  viewport source:  visualViewport.height (fallback: innerHeight)
  recreate on:      visualViewport resize (debounce 50ms) — TAK
  PIN-DISABLE-01:   PASS — pause() nie woła ST.disable()/ST.kill()

AUTO-FIXy ZASTOSOWANE (P2A):
  1. TS-LINT-UNUSED-01: dead code usunięty (6 items: hfListeners, globalInsertionSort,
     sortedIndices, PALETTE_C/ALL_PALETTES/activePaletteKey/swapTargetPalette chain)
     DEBUG_MODE zachowany (Factory infrastructure pattern).
  2. NULL-GUARD-01: guardy na particle canvas, b1, b2||b3 (early return z safe handle)
  3. ST-REFRESH-01: section-in-view IO (jednorazowy) + layout-settle timer (1000ms)
     P1 miał refresh_scaffold=partial → teraz kompletny.
  4. Factory IO gating (Ścieżka 1): IO na [data-gating-target], rootMargin 0.5×VH,
     recreate na visualViewport resize, named handlers (INP-LEAK-01 safe)

BRAMKI BEZPIECZEŃSTWA:
  B-CPU-03  [REVIEW]  idempotencja pause/resume:          OK (if(_paused)return/if(!_paused)return)
  B-CPU-04  [REVIEW]  listener options:                    N/A (resume() reattach via tickerFns+gsap.ticker.add, nie addEventListener)
  B-CPU-05  [REVIEW]  cleanup order:                       OK (_killed→pause→cleanups→gsapInstances→tickers→timers→observers→state reset)
  B-VEL-01  [—]       velocity reset resume:               N/A (brak velocity/physics loop)
  B-VEL-02  [—]       velocity init:                       N/A (brak velocity/physics loop)
  B-REF-01  [STATIC]  requestRefresh:                      OK (brak ST.refresh() w kodzie sekcji; ST-REFRESH-01 scaffold dodany przez P2A)
  B-REF-02  [STATIC]  refresh kaskada:                     OK (brak requestRefresh w onRefresh; onRefresh invaliduje geo cache + reconcile)
  B-GEO-01  [STATIC]  geometry:                            N/A (pinned fullscreen, fixed layout)
  B-PIN-01  [REVIEW]  pin refresh:                         OK (pin:true, geo cache invalidated w onRefresh, ST-REFRESH-01 dodaje refresh po section-in-view)
  B-CSS-01  [STATIC]  CSS vs GSAP:                         OK (zero CSS transition w sekcji)
  B-ISO-01  [—]       isolation:                           NIE — owner zachowuje blend z tłem. WARNING: brak stacking context.
            3× mix-blend-mode celowo mieszają z tłem (luminosity, multiply, color-burn).
            isolation:isolate pominięte — akceptowane ryzyko braku izolacji.
  CLN-01    [RUNTIME]  cleanup:                            PASS (owner: KILL → liczniki = 0, brak orphans)
  ST-01     [REVIEW]  ST idempotent:                       OK (onEnter/onLeave = boolean flag, onRefresh = cache+reconcile, onUpdate = freeze/clamp)
  INP-01    [STATIC]  passive listeners:                   OK (brak bezpośrednich wheel/touchmove)
  INP-02    [STATIC]  pointermove:                         N/A (brak pointermove/mousemove per-frame)
  MED-01    [STATIC]  media attributes:                    N/A (brak video/audio, canvas = programistyczny)
  VAN-SPLIT [REVIEW]  SplitText:                           OK (custom splitIntoChars, nie SplitText plugin, brak revert potrzebny)
  J-GPU-01  [REVIEW]  GPU cleanup:                         OK (pause: willChange:'auto', resume: przywraca willChange:'transform,opacity' na B2/B3 chars)
  SM-01     [RUNTIME]  INIT AGAIN 3×:                      PASS (owner: stabilne liczniki po 3 cyklach)
  SM-02     [REVIEW]  double callbacks:                    OK (onEnter/onLeave = boolean, Observer = guards, snap1 magnet = committed guard)
  INIT-CPU-01 [REVIEW] long task init:                     OK (brak pętli >100 iter synchronicznie w init; particles tworzone w resize callback po fonts.ready)
  GL-CPU-01 [REVIEW]  GL loop:                             N/A (brak WebGL)
  GL-CPU-02 [REVIEW]  GL pause:                            N/A
  GL-MEM-01 [REVIEW]  GL dispose:                          N/A
  GL-WARM-01 [REVIEW] WARM state + compileAsync:           N/A
  GL-LC-01  [REVIEW]  lifecycle COLD/WARM/HOT/OFF:         N/A
  GL-IDLE-01 [REVIEW] rIC fallback:                        N/A
  GL-MEM-02 [REVIEW]  GL resume:                           N/A
  GL-TIER-01 [REVIEW] GL tier0:                            N/A
  GL-OFF-01 [MANIFEST-CHECK] timed dispose:                N/A
  B-LC-RET-01 [STATIC] return handle:                      OK (return { kill, pause, resume, _s } na L4221)
  ST-CLEAN-01 [REVIEW] ST cleanup scope:                   OK (kill: gsapInstances.forEach → scrollTrigger.kill() + revert() + kill(). Jedyny ST = KINETIC_PIN.)
  INP-LEAK-01 [STATIC] named handlers:                     OK (wszystkie addEventListener mają named refs + cleanup w cleanups[])
  INIT-DOM-01 [STATIC] DOMContentLoaded w init:            OK (init() nie rejestruje DOMContentLoaded; auto-init wrapper jest shell JS)
  DEV-DEL-01  [REVIEW] overlay side-effects:               OK (overlay DOM w FACTORY:DEV-OVERLAY:START/END, JS w if(DEBUG_MODE) w DOMContentLoaded, zero side-effectów w init)

PERFORMANCE (P2A):
  PERF-01   [—]       media attributes:    N/A (brak mediów)
  PERF-02   [—]       contain layout:      SKIP (pinned fullscreen 100lvh — contain mogłoby zakłócić pin)
  PERF-03   [DEV]     telemetria:          PENDING (otwórz reference.html?debug=1 i sprawdź overlay)
  PERF-W2   [WARNING] 3× canvas bez HTML width/height (JS resize handlers, initial 300×150 default, niski priorytet — opacity:0 na starcie)
  PERF-W6   [WARNING] 5× gsap.ticker.add (gate+DPR lightweight, particle/tunnel/cylinder per-subsystem — celowa architektura IIFE)
  PERF-W7   [WARNING] Google Fonts URL bez explicit &subset=latin-ext (auto-served via unicode-range, low risk)

ANTI-FOUC (P3 wypełni):
  PERF-04   [—]       inline styles JSX:  PENDING (P3)

DYNAMIC IMPORT (P3 wypełni):
  PERF-05   [—]       dynamic import:     NIE (owner decision: potwierdzone)

STACK-HARNESS: WYGENEROWANY
  Warunki P2B-01 spełnione:
    hasPin: true ✓
    hasSnap: true ✓
    geometryContract: 'pinned-narrative' (nie 'normal') ✓
    refreshSignals: ['section-in-view', 'layout-settle'] (niepuste) ✓
  CSS-SCOPE-01: PASS — wszystkie selektory CSS scoped do #kinetic-section
  scrollRuntime: stub (Lenis init + harness scheduleRefresh) — INT-* testują ST drift, NIE scrollRuntime policy
  Nota: Lenis zainicjalizowany w harnesie (wymagany dla snap system — lenis.scrollTo).
        scrollRuntime.requestRefresh deleguje do scheduleRefresh (debounce 150ms).

  INT-01    [RUNTIME]  accordion expand:  PASS (owner)
  INT-02    [RUNTIME]  pin above:         PASS (owner)
  INT-03    [RUNTIME]  height change:     PASS (owner)
  INT-04    [RUNTIME]  positioning invariance (stack.html ze spacerami 150vh/100vh):
                       PASS (owner)
            Instrukcja: otwórz reference.html → zapamiętaj moment wejścia animacji.
            Otwórz kinetic.stack.html (spacery 150vh nad sekcją) → przewiń do sekcji → odśwież → przewiń.
            PASS: animacje identyczne niezależnie od pozycji w dokumencie.
            FAIL: start/end przesunięty lub sekcja "odpływa" po spacerze.
            Najczęstsze przyczyny FAIL: trigger na inner element, document-relative start/end,
            brak ST-REFRESH-01 scaffold.

EXTERNAL CDN LIBRARIES:
  https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js → gsap
  https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js → ScrollTrigger
  https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js → Lenis

GSAP VERSION NOTE:
  reference.html + stack.html: 3.12.5 (CDN — sandbox version)
  Produkcja: 3.12.7 (npm — spójne środowisko)
  Smoke test w repo wymagany po konwersji React (P3, DEVELOPER_HANDOFF.md)

SECTION POSITION INVARIANCE:
  sensitiveTo geometry-above:  NIE
  trigger_root:                PASS (trigger = container)
  offset_model:                viewport-relative ("top top", "+=" dynamic)
  refresh_mechanism:           geometryRefresh:self + refreshSignals:['section-in-view','layout-settle']
  ST-REFRESH scaffold:         AUTO-FIXED (P2A: partial→kompletny)
  INT-04 positioning test:     PASS (owner)

GUARD PRESERVATION [P2A-03]:
  preserveExistingGuards: true
  Guardy zachowane (9/9):
    1. _sectionVisible (ST onEnter/onLeave gating) ✓
    2. freezeFinal (blokuje resize-handlery i snap forward na końcu) ✓
    3. mobileResizeLock (blokuje snap podczas toolbar/resize) ✓
    4. adaptiveDPR._scrollLockUntil (blokuje DPR change mid-scroll) ✓
    5. svh/lvh measurement probe (jednorazowy) ✓
    6. _sm zone reconciliation (state machine v3) ✓
    7. IS_TOUCH && freezeFinal guards w resize functions ✓
    8. _s._killed guards w ticker functions ✓
    9. document.hidden guards w ticker functions ✓
  Guardy dodane: NULL-GUARD-01 (particle canvas, b1, b2||b3), IO-SAFE-01 (Factory IO, ST-REFRESH IO)

WERDYKT: PASS — wszystkie bramki OK (STATIC/REVIEW + RUNTIME potwierdzone przez właściciela).
  Decyzje właściciela zamknięte:
    - B-ISO-01: isolation_override = NIE (blend z tłem celowy, WARNING zaakceptowany)
    - dynamicImport: NIE (potwierdzone)
  RUNTIME checks: CLN-01 PASS, SM-01 PASS, SM-02 PASS, INT-01 PASS, INT-02 PASS, INT-03 PASS, INT-04 PASS

═══ RAW P2A EVIDENCE (verbatim) ═══
=== EVIDENCE PACK — P2A ===
Slug: kinetic
Typ: B
CPU Gating: Ścieżka 1, IO pin-safe, rootMargin: 0.5×VH clamp(200,1200)px

── AUTO-FIXy zastosowane ──
1. TS-LINT-UNUSED-01: Usunięto dead code:
   - hfListeners (declared, never used)
   - globalInsertionSort (function, commented-out caller only)
   - sortedIndices (Int16Array, allocated but never read — sort disabled)
   - PALETTE_C, ALL_PALETTES, activePaletteKey (swapTargetPalette chain)
   - swapTargetPalette (function, never called, not exported)
   DEBUG_MODE w init() — ZACHOWANY: Factory infrastructure pattern (dostępny
   dla conditional debug logging w scope init(); używany w DOMContentLoaded overlay).

2. NULL-GUARD-01: Dodano guardy:
   - particle canvas: if (!canvas) return; (IIFE early exit)
   - b1: if (!b1) { kill(); return {kill,pause,resume}; }
   - b2/b3: if (!b2||!b3) { kill(); return {kill,pause,resume}; }

3. ST-REFRESH-01: Dodano (P1: refresh_scaffold=partial → AUTO-FIXED):
   - section-in-view IO (jednorazowy, threshold:0, rootMargin:0px)
   - layout-settle timer (1000ms po init)
   Oba w observers[]/cleanups[]/timerIds[].

4. Factory IO gating (Ścieżka 1):
   - IntersectionObserver obserwuje [data-gating-target] (.content-wrapper)
   - rootMargin: 0.5×VH, clamp 200-1200px
   - Recreate na visualViewport resize (debounce 50ms)
   - Named handlers (_onFactoryVVResize) — INP-LEAK-01 safe
   - _killed guard w recreate
   - IO-SAFE-01 guard w callback (entries[0] check)

── Bramki hardeningu ──
B-CPU-01  [AUTO-FIX]  IO gating dodany — Ścieżka 1 (IO-safe, pin — no disable)
PIN-DISABLE-01 [STATIC] PASS — pause() nie woła ST.disable()/ST.kill(). Odłącza tickery + pauzuje timelines.
B-LC-RET-01 [STATIC]  PASS — return { kill, pause, resume, _s } na L4221 (ostatnia instrukcja init)
B-CPU-03  [REVIEW]    PASS — pause: if(_paused)return; resume: if(!_paused)return; → idempotentne
B-CPU-04  [REVIEW]    N/A — resume() nie dodaje addEventListener (reattach via tickerFns.forEach + gsap.ticker.add)
B-CPU-05  [REVIEW]    PASS — kill: _killed flag → pause(tickers+TL) → cleanups(listeners) → gsapInstances(TL+ST) → tickers → timers → observers → state reset. _s._killed guard zapobiega callback fire during cleanup.
B-VEL-01  [AUTO-FIX]  N/A — brak velocity/physics loop
B-VEL-02  [AUTO-FIX]  N/A — brak velocity/physics loop
B-REF-01  [STATIC]    PASS — brak ScrollTrigger.refresh() w kodzie sekcji. Refresh scaffold dodany przez ST-REFRESH-01 (jednorazowy IO + timer).
B-REF-02  [STATIC]    PASS — brak refresh kaskady (brak requestRefresh w onRefresh). onRefresh (L3069) invaliduje geo cache + reconcile — bez refresh call.
B-GEO-01  [STATIC]    N/A — sekcja nie zmienia wysokości (pinned fullscreen, fixed layout)
B-PIN-01  [REVIEW]    PASS — sekcja ma pin:true, snap system zarządzany przez state machine + lenis.scrollTo. Geometry cache invalidated w onRefresh (L3071). ST-REFRESH-01 dodaje refresh po section-in-view.
── WebGL L1 (STOP) ──
GL-CPU-01 [REVIEW]    N/A — brak WebGL (3× canvas 2D)
GL-CPU-02 [REVIEW]    N/A
GL-MEM-01 [REVIEW]    N/A
GL-WARM-01 [REVIEW]   N/A
GL-LC-01  [REVIEW]    N/A
GL-IDLE-01 [REVIEW]   N/A
── WebGL L2 (WARNING) ──
GL-MEM-02 [REVIEW]    N/A
GL-TIER-01[REVIEW]    N/A
GL-CTX-01 [REVIEW]    N/A
B-CSS-01  [STATIC]    PASS — brak CSS transition w sekcji. Zero konfliktu transition vs GSAP.
B-ISO-01  [AUTO-FIX]  OWNER-DECISION-PENDING(blend/filter)
  isolation:isolate wstrzymane — wykryto:
  - mix-blend-mode: luminosity na .nigdy-glow
  - mix-blend-mode: multiply na .blob
  - mix-blend-mode: color-burn na #kinetic-particle-qmark-canvas
  Owner musi zdecydować w owner_decisions:
    isolation_override: TAK → akceptuję utratę blend z tłem → dodajemy isolation:isolate
    isolation_override: NIE → celowo miesza z tłem → pomijamy, WARNING o braku stacking context
CLN-01    [RUNTIME]   PENDING(owner) — weryfikacja przez KILL button w ?debug=1
ST-01     [REVIEW]    PASS — onEnter/onLeave (L3034-3037) ustawiają boolean _sectionVisible. Brak side-effect (track/fetch/analytics). onRefresh (L3069) invaliduje cache + reconcile — bezpieczne. onUpdate (L3042) zarządza freeze/clamp/reconcile — stanowe, nie side-effectowe.
ST-02     [STATIC]    PASS — trigger: container (L3019), start: "top top" (L3020). Viewport-relative, bez px offsetów.
ST-CLEAN-01 [REVIEW]  PASS — kill() L4111-4116: gsapInstances.forEach → scrollTrigger.kill() + revert() + kill(). Jedyny ST w sekcji = KINETIC_PIN, powiązany z pinnedTl w gsapInstances. Brak globalnego killAll().
INP-01    [STATIC]    PASS — brak bezpośrednich wheel/touchmove/touchstart addEventListener. Observer (L3268) przez ScrollTrigger.observe() (internal handling). Resize listeners nie wymagają passive.
INP-02    [STATIC]    N/A — brak pointermove/mousemove per-frame
INP-LEAK-01 [STATIC]  PASS — wszystkie addEventListener mają named references:
  - armMobileResizeLock → removeEventListener w cleanups (L775-778)
  - _resizeParticle → removeEventListener w cleanups (L2094)
  - resize (cylinder) → removeEventListener w cleanups (L2503)
  - _resizeTunnel → removeEventListener w cleanups (L2578)
  - positionNigdyGlow → removeEventListener w cleanups (L3984)
  - _lenisSnap1Handler → lenis.off w cleanups (L3341)
  - _kineticObserver → .kill() w cleanups (L3283) + kill() (L4148)
  - Factory _onFactoryVVResize → removeEventListener w cleanups (dodany P2A)
INIT-DOM-01 [STATIC]  PASS — init() nie rejestruje DOMContentLoaded/window.load. Auto-init wrapper (L4160) jest shell JS, nie w init().
MED-01    [STATIC]    N/A — brak video/audio. Canvas = programistyczny (nie media asset).
DEV-DEL-01  [REVIEW]  PASS — overlay DOM między FACTORY:DEV-OVERLAY:START/END markerami. Cały overlay JS (event listeners, telemetria) wewnątrz if(DEBUG_MODE) w DOMContentLoaded (poza init()). Zero side-effectów w sekcji init poza DEBUG_MODE check. Mechaniczne wycięcie bloku overlay nie zmienia zachowania sekcji.
VAN-SPLIT [REVIEW]    PASS — splitIntoChars jest custom split (nie SplitText plugin). Brak revert() potrzebny — DOM jest jednorazowo transformowany. kill() nie wymaga revert splitów (sekcja jest usuwana).
J-GPU-01  [REVIEW]    PASS — pause() ustawia willChange:'auto' na .anim-char (L4087). Resume przywraca willChange:'transform,opacity' na Block 2 i Block 3 header chars (L4099-4102). Scrub timeline = nie one-shot → willChange zarządzane per-phase, nie onComplete.
SM-01     [RUNTIME]   PENDING(owner) — weryfikacja przez INIT AGAIN 3× w ?debug=1
SM-02     [REVIEW]    PASS — onEnter/onLeave (L3034-3037) ustawiają boolean flag — brak analytics/track. Observer callback (L3272-3279) deleguje do _handleIntent z guardami (state=snapping, cooldown, mobileResizeLock). Snap1 magnet (L3292-3336) ma guard: committedIndex>=0 → milczy na zawsze po committed.
INIT-CPU-01[REVIEW]   PASS — brak pętli >100 iteracji w init(). createParticles: max 2000 particles (maxCapacity 4000 z safety clamp), ale wywoływane w resize callback (nie synchronicznie w init flow — document.fonts.ready.then). Bucket prealokacja: 128 × 3 arrays = O(384) — liniowe, nie N².

── RUNTIME SAFETY ──
TS-LINT-UNUSED-01 [STATIC] PASS — dead code usunięty (6 items). DEBUG_MODE zachowany (Factory infrastructure).
NULL-GUARD-01     [STATIC] PASS — guardy dodane na: particle canvas, b1, b2||b3.
IO-SAFE-01        [STATIC] PASS — Factory IO callback: if(!entries[0])return; ST-REFRESH IO: if(!entries[0]||!entries[0].isIntersecting)return;
ARRAY-ASSIGN-01   [STATIC] N/A — brak dynamicznych indeksów z DOM/atrybutów w array assignment. pColorAIdx/pColorBIdx indeksowane Math.floor(Math.random()*4) — bounded [0,3].
HTML-FONTSIZE-01  [STATIC] PASS — brak html/root font-size w CSS sekcji.

── SECTION POSITION INVARIANCE ──
POS-ROOT-01  [STATIC]    PASS — trigger: container, start: "top top", end: dynamic "+=" (viewport-relative)
ST-REFRESH   [AUTO-FIX]  PARTIAL→AUTO-FIXED
  P1 miał: refresh_scaffold=partial (onRefresh geo cache + reconcile, brak layout-settle)
  P2A dodał: section-in-view IO + layout-settle timer (1000ms)

CPU GATING koegzystencja:
  Sekcja ma wewnętrzny ST-native gating (_sectionVisible via onEnter/onLeave).
  Fabryka dodaje zewnętrzny IO gating (rootMargin 0.5×VH clamp 200-1200px).
  Koegzystencja bezpieczna: B-CPU-03 PASS (idempotent pause/resume) + ENT-LC-06 PASS (z P1).
  Asymetria: Factory IO (VH-based rootMargin, ~400px) > internal ST (0px, viewport exact).
  Korzystny wzorzec: Factory pre-pauses zanim sekcja wyjdzie z viewportu.
  Resume order: Factory IO resume → tickery wznowione → ST onEnterBack → _sectionVisible=true → tickGate przepuszcza.

── Media attributes ──
Brak mediów (0 img, 0 video). 3× canvas = programistyczne.

── Performance Warnings ──
PERF-W7 [WARNING]: Polskie znaki (ąćęłńóśźż) w HTML + Google Fonts URL bez explicit &subset=latin-ext.
PERF-W2 [WARNING]: 3× <canvas> bez HTML width+height atrybutów.
PERF-W6 [WARNING]: 5× gsap.ticker.add w sekcji.

── RUNTIME bramki — wymagają ręcznej weryfikacji właściciela ──
CLN-01  PENDING(owner) — kliknij KILL w ?debug=1
SM-01   PENDING(owner) — kliknij INIT AGAIN 3× w ?debug=1

GUARD PRESERVATION [P2A-03]:
  preserveExistingGuards: true
  Guardy zachowane: TAK (wszystkie 9)
  Guardy dodane: NULL-GUARD-01 (particle canvas, b1, b2||b3), IO-SAFE-01 (Factory IO, ST-REFRESH IO)
═══ KONIEC RAW P2A EVIDENCE ═══
═══════════════════════════════════════
