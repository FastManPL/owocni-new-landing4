'use client';

import ConversionCalculator from './ConversionCalculator.PRE-FACTORY';

/** Silnik kalkulatora — lazy z RSC shell (`KalkulatorSection`). */
export function KalkulatorSectionClient() {
  return <ConversionCalculator />;
}
