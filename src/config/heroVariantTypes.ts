import type { ReactNode } from 'react';

/** Kontrakt DCI Hero — współdzielony przez server (generated) i client (HeroSection). */
export type HeroVariant = {
  h1: ReactNode;
  h2: ReactNode;
  metaTitle: string;
  metaDescription: string;
  ogTitle: string;
  ogDescription: string;
};
