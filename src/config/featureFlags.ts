/**
 * Feature flags (build-time / runtime booleans).
 *
 * **SHOW_KINETIC_SECTION** — `false`: sekcja Kinetic nie jest montowana na home,
 * chunk silnika się nie ładuje. Ustaw `true`, aby przywrócić (patrz `page.tsx` + `KineticBridgeLayer.tsx`).
 */
export const SHOW_KINETIC_SECTION = false;
