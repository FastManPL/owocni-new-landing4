UWAGA: ten katalog (generacja ze *.stack.html) jest przestarzały.
Aktualny podgląd z Reacta: preview-html/kinetic-blok45-lp-react/ — npm run preview:kinetic-blok45-html

---
KINETIC-BLOK45-LP.html — stary build + obrazy (nieaktualizowany skrypt).

Podgląd lokalnie (file:// ogranicza importmap/Three — użyj serwera):
  cd preview-html/kinetic-blok45-lp
  npx --yes serve .

Do czatu AI: spakuj ZIP całego folderu (HTML + 4 pliki obrazów) albo wklej HTML jeśli mieści się w limicie kontekstu.
