Podgląd z AKTUALNEGO Reacta (KineticEngine.tsx, Blok45Engine.tsx, scrollRuntime, Bridge).
Bez plików *.stack.html.

Regeneracja:
  npm run preview:kinetic-blok45-html
  (node scripts/build-kinetic-blok45-react-preview-html.mjs)

Pliki:
  index.html       — szkielet + style sekcji (CSS z plików .css sekcji)
  preview-app.js   — zbudowany bundle (React + GSAP + Three + silniki)

Podgląd:
  cd preview-html/kinetic-blok45-lp-react && npx --yes serve .

ZIP do czatu: cały folder (index.html + preview-app.js + obrazy Ludzie*).
